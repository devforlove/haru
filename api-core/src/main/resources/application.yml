
spring:
  datasource:
    url: jdbc:mysql://localhost:17001/haru-db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: bedc003a9899e7c1fddf9a2b0940d24a
            client-secret: OFP3y0m1kqid0gxi8aJRrwOhtAmJETJU
            redirect-uri: http://localhost:8080/login/oauth2/code/kakao
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            scope:
              - profile_nickname
              - profile_image
            client-name: kakao

            # kakao provider 설정
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-info-authentication-method: header
            user-name-attribute: id

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      acks: all
      # 성능/신뢰성 튜닝
      properties:
        enable.idempotence: true
        max.in.flight.requests.per.connection: 5
        linger.ms: 20
        batch.size: 32768
        compression.type: lz4
        delivery.timeout.ms: 120000
        retries: 2147483647
        request.timeout.ms: 30000
    properties:
      spring.json.add.type.headers: false
    template:
      default-topic: user-registered-topic

jwt:
  secret: adf4aafsaf3lzalbvppamvpdfasadfe3pdpsafa2kadfkfafdasmsclxlxefewafvcz323f
  expiration_time: 1800000